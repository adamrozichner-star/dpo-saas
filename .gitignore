tsconfig.tsbuildinfo

# Sentry
.env.sentry-build-plugin

.vercel

# Build artifacts and dependencies (previously untracked but not ignored)
.next/
node_modules/
.env*.local

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# macOS + local design-asset drops
.DS_Store
Deepo_Design_System.zip
