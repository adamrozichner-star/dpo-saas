tsconfig.tsbuildinfo

# Sentry
.env.sentry-build-plugin

.vercel

# Build artifacts and dependencies (previously untracked but not ignored)
.next/
node_modules/
.env*.local

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# Design-system reference archive (the extracted deepo-brand/design-system/
# folder is kept locally and left untracked; the raw zip is never committed)
Deepo_Design_System.zip
.DS_Store
**/.DS_Store
